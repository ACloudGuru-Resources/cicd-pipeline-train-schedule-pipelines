pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        echo 'Running build automation'
      }
    }
    stage("Test") {
      steps {
        echo 'Testing the application'
      }
    }
    stage("Deploy") {
      steps {
        echo 'Deploying the application'
      }
    }
  }
}
